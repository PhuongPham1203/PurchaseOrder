import { ValidateSCCEmailDirective } from './validate-sccemail.directive';

describe('ValidateSCCEmailDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateSCCEmailDirective();
    expect(directive).toBeTruthy();
  });
});
