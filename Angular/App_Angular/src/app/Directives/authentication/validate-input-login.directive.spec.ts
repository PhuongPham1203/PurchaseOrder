import { ValidateInputLoginDirective } from './validate-input-login.directive';

describe('ValidateInputLoginDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateInputLoginDirective();
    expect(directive).toBeTruthy();
  });
});
