import { RoleAccountDirective } from './role-account.directive';

describe('RoleAccountDirective', () => {
  it('should create an instance', () => {
    const directive = new RoleAccountDirective();
    expect(directive).toBeTruthy();
  });
});
